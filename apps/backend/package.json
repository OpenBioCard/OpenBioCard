{
  "name": "openbiocard-backend",
  "version": "1.0.0",
  "description": "OpenBioCard Backend",
  "main": "index.tsx",
  "scripts": {
    "dev": "wrangler dev",
    "build": "pnpm --filter openbiocard-frontend build && cp -r ../frontend/dist ./dist && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.10.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.55.0"
  }
}